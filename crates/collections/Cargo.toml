[package]
name = "chess-kit-collections"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "collections_runtime"
harness = false

[[bench]]
name = "collections_memory"
harness = false
